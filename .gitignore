node_modules/
dist/
*.db
*.db-wal
*.db-shm
.env
.env.local
*.tsbuildinfo
apps/dashboard/src/routeTree.gen.ts

# Claude Code and SDD scratch workspaces
.claude/
.superpowers/
graphify-out/
# Copied in by the release workflow; the repository has one at the root.
packages/plugin-api/LICENSE
packages/dashboard-sdk/LICENSE

# Scratch probes and mutation harnesses. Two of these were committed once by a
# `git add -A`, and one of them was the codemod that had just rewritten five
# source files — so the rule is that they cannot be staged, not that you should
# remember to delete them.
.tmp-*
