[test]
# The dashboard suite registers a DOM, and registration mutates process-wide
# globals. Run inside the root suite it leaks a document into every other file
# sharing the process — which surfaced as a one-in-several-runs failure, not as
# an obvious break.
pathIgnorePatterns = ["apps/dashboard/test/**"]
